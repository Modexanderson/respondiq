rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated admin SDK (backend) full access
    // Browser clients cannot directly access Firestore
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
